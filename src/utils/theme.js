import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#000000", light: "#000116" },
    danger: { main: "#fd1706", dark: "#da0000" },
    info: { main: "#FFFFFF", dark: "#A9A9A9" },
  },
});

export default theme;
